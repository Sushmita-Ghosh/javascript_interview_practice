/**
 * setInterval - to run a function at a fixed interval of time until clearInterval is called, it will keep on running unlike setTimeout, which will run only once
 * clearInterval - to stop the interval
 */
